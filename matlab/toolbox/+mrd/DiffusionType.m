% This file was generated by the "yardl" tool. DO NOT EDIT.

classdef DiffusionType < handle
  properties
    gradient_direction
    bvalue
  end

  methods
    function self = DiffusionType(kwargs)
      arguments
        kwargs.gradient_direction = mrd.GradientDirectionType();
        kwargs.bvalue = single(0);
      end
      self.gradient_direction = kwargs.gradient_direction;
      self.bvalue = kwargs.bvalue;
    end

    function res = eq(self, other)
      res = ...
        isa(other, "mrd.DiffusionType") && ...
        isequal({self.gradient_direction}, {other.gradient_direction}) && ...
        isequal({self.bvalue}, {other.bvalue});
    end

    function res = ne(self, other)
      res = ~self.eq(other);
    end

    function res = isequal(self, other)
      res = all(eq(self, other));
    end
  end

  methods (Static)
    function z = zeros(varargin)
      elem = mrd.DiffusionType();
      if nargin == 0
        z = elem;
        return;
      end
      sz = [varargin{:}];
      if isscalar(sz)
        sz = [sz, sz];
      end
      z = reshape(repelem(elem, prod(sz)), sz);
    end
  end
end
