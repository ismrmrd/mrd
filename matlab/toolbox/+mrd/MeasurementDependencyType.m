% This file was generated by the "yardl" tool. DO NOT EDIT.

classdef MeasurementDependencyType < handle
  properties
    dependency_type
    measurement_id
  end

  methods
    function self = MeasurementDependencyType(kwargs)
      arguments
        kwargs.dependency_type = "";
        kwargs.measurement_id = "";
      end
      self.dependency_type = kwargs.dependency_type;
      self.measurement_id = kwargs.measurement_id;
    end

    function res = eq(self, other)
      res = ...
        isa(other, "mrd.MeasurementDependencyType") && ...
        isequal({self.dependency_type}, {other.dependency_type}) && ...
        isequal({self.measurement_id}, {other.measurement_id});
    end

    function res = ne(self, other)
      res = ~self.eq(other);
    end

    function res = isequal(self, other)
      res = all(eq(self, other));
    end
  end

  methods (Static)
    function z = zeros(varargin)
      elem = mrd.MeasurementDependencyType();
      if nargin == 0
        z = elem;
        return;
      end
      sz = [varargin{:}];
      if isscalar(sz)
        sz = [sz, sz];
      end
      z = reshape(repelem(elem, prod(sz)), sz);
    end
  end
end
