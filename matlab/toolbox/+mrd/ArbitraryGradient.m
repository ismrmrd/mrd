% This file was generated by the "yardl" tool. DO NOT EDIT.

classdef ArbitraryGradient < handle
  % An arbitrary gradient event
  properties
    % ID of the gradient event. Must be unique among all arbitrary and trapezoidal gradient events.
    id
    % Peak amplitude in Hz/m
    amp
    % The amplitude at the start of the gradient.
    first
    % The amplitude at the end of the gradient.
    last
    % Shape ID for the gradient shape
    shape_id
    % Shape ID for the time sampling points, specified in the units of GradientRasterTime.
    % 0 means default time raster, -1 means 1/2 of the default time raster (gradient oversampling case).
    time_id
    % Delay before starting the gradient, specified in microseconds
    delay
  end

  methods
    function self = ArbitraryGradient(kwargs)
      arguments
        kwargs.id = int32(0);
        kwargs.amp = double(0);
        kwargs.first = double(0);
        kwargs.last = double(0);
        kwargs.shape_id = int32(0);
        kwargs.time_id = int32(0);
        kwargs.delay = uint64(0);
      end
      self.id = kwargs.id;
      self.amp = kwargs.amp;
      self.first = kwargs.first;
      self.last = kwargs.last;
      self.shape_id = kwargs.shape_id;
      self.time_id = kwargs.time_id;
      self.delay = kwargs.delay;
    end

    function res = eq(self, other)
      res = ...
        isa(other, "mrd.ArbitraryGradient") && ...
        isequal({self.id}, {other.id}) && ...
        isequal({self.amp}, {other.amp}) && ...
        isequal({self.first}, {other.first}) && ...
        isequal({self.last}, {other.last}) && ...
        isequal({self.shape_id}, {other.shape_id}) && ...
        isequal({self.time_id}, {other.time_id}) && ...
        isequal({self.delay}, {other.delay});
    end

    function res = ne(self, other)
      res = ~self.eq(other);
    end

    function res = isequal(self, other)
      res = all(eq(self, other));
    end
  end

  methods (Static)
    function z = zeros(varargin)
      elem = mrd.ArbitraryGradient();
      if nargin == 0
        z = elem;
        return;
      end
      sz = [varargin{:}];
      if isscalar(sz)
        sz = [sz, sz];
      end
      z = reshape(repelem(elem, prod(sz)), sz);
    end
  end
end
