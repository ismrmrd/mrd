% This file was generated by the "yardl" tool. DO NOT EDIT.

classdef CoilLabelType < handle
  properties
    coil_number
    coil_name
  end

  methods
    function self = CoilLabelType(kwargs)
      arguments
        kwargs.coil_number = uint32(0);
        kwargs.coil_name = "";
      end
      self.coil_number = kwargs.coil_number;
      self.coil_name = kwargs.coil_name;
    end

    function res = eq(self, other)
      res = ...
        isa(other, "mrd.CoilLabelType") && ...
        isequal(self.coil_number, other.coil_number) && ...
        isequal(self.coil_name, other.coil_name);
    end

    function res = ne(self, other)
      res = ~self.eq(other);
    end
  end

  methods (Static)
    function z = zeros(varargin)
      elem = mrd.CoilLabelType();
      if nargin == 0
        z = elem;
        return;
      end
      sz = [varargin{:}];
      if isscalar(sz)
        sz = [sz, sz];
      end
      z = reshape(repelem(elem, prod(sz)), sz);
    end
  end
end
