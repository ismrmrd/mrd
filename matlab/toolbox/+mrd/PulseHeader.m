% This file was generated by the "yardl" tool. DO NOT EDIT.

classdef PulseHeader < handle
  properties
    % Clock time stamp (e.g. nanoseconds since midnight)
    pulse_time_stamp_ns
    % Channel numbers
    channel_order
    % Sample time in ns
    sample_time_ns
    % Pulse calibration (rad/s/V). Can be here or as a calB1Map calibration image or neither
    pulse_calibration
  end

  methods
    function self = PulseHeader(kwargs)
      arguments
        kwargs.pulse_time_stamp_ns = uint64(0);
        kwargs.channel_order = uint32.empty();
        kwargs.sample_time_ns = uint32(0);
        kwargs.pulse_calibration = yardl.None;
      end
      self.pulse_time_stamp_ns = kwargs.pulse_time_stamp_ns;
      self.channel_order = kwargs.channel_order;
      self.sample_time_ns = kwargs.sample_time_ns;
      self.pulse_calibration = kwargs.pulse_calibration;
    end

    function res = eq(self, other)
      res = ...
        isa(other, "mrd.PulseHeader") && ...
        isequal(self.pulse_time_stamp_ns, other.pulse_time_stamp_ns) && ...
        isequal(self.channel_order, other.channel_order) && ...
        isequal(self.sample_time_ns, other.sample_time_ns) && ...
        isequal(self.pulse_calibration, other.pulse_calibration);
    end

    function res = ne(self, other)
      res = ~self.eq(other);
    end
  end

  methods (Static)
    function z = zeros(varargin)
      elem = mrd.PulseHeader();
      if nargin == 0
        z = elem;
        return;
      end
      sz = [varargin{:}];
      if isscalar(sz)
        sz = [sz, sz];
      end
      z = reshape(repelem(elem, prod(sz)), sz);
    end
  end
end
