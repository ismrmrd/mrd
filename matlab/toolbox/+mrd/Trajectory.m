% This file was generated by the "yardl" tool. DO NOT EDIT.

classdef Trajectory < uint64
  methods (Static)
    function v = CARTESIAN
      v = mrd.Trajectory(0);
    end
    function v = EPI
      v = mrd.Trajectory(1);
    end
    function v = RADIAL
      v = mrd.Trajectory(2);
    end
    function v = GOLDENANGLE
      v = mrd.Trajectory(3);
    end
    function v = SPIRAL
      v = mrd.Trajectory(4);
    end
    function v = OTHER
      v = mrd.Trajectory(5);
    end

    function z = zeros(varargin)
      elem = mrd.Trajectory(0);
      if nargin == 0
        z = elem;
        return;
      end
      sz = [varargin{:}];
      if isscalar(sz)
        sz = [sz, sz];
      end
      z = reshape(repelem(elem, prod(sz)), sz);
    end
  end
end
