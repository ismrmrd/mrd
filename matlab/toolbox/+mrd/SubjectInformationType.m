% This file was generated by the "yardl" tool. DO NOT EDIT.

classdef SubjectInformationType < handle
  properties
    patient_name
    patient_weight_kg
    patient_height_m
    patient_id
    patient_birthdate
    patient_gender
  end

  methods
    function self = SubjectInformationType(kwargs)
      arguments
        kwargs.patient_name = yardl.None;
        kwargs.patient_weight_kg = yardl.None;
        kwargs.patient_height_m = yardl.None;
        kwargs.patient_id = yardl.None;
        kwargs.patient_birthdate = yardl.None;
        kwargs.patient_gender = yardl.None;
      end
      self.patient_name = kwargs.patient_name;
      self.patient_weight_kg = kwargs.patient_weight_kg;
      self.patient_height_m = kwargs.patient_height_m;
      self.patient_id = kwargs.patient_id;
      self.patient_birthdate = kwargs.patient_birthdate;
      self.patient_gender = kwargs.patient_gender;
    end

    function res = eq(self, other)
      res = ...
        isa(other, "mrd.SubjectInformationType") && ...
        isequal(self.patient_name, other.patient_name) && ...
        isequal(self.patient_weight_kg, other.patient_weight_kg) && ...
        isequal(self.patient_height_m, other.patient_height_m) && ...
        isequal(self.patient_id, other.patient_id) && ...
        isequal(self.patient_birthdate, other.patient_birthdate) && ...
        isequal(self.patient_gender, other.patient_gender);
    end

    function res = ne(self, other)
      res = ~self.eq(other);
    end
  end

  methods (Static)
    function z = zeros(varargin)
      elem = mrd.SubjectInformationType();
      if nargin == 0
        z = elem;
        return;
      end
      sz = [varargin{:}];
      if isscalar(sz)
        sz = [sz, sz];
      end
      z = reshape(repelem(elem, prod(sz)), sz);
    end
  end
end
