% This file was generated by the "yardl" tool. DO NOT EDIT.

classdef ArrayType < uint64
  methods (Static)
    function v = SPIN_DENSITY_MAP
      v = mrd.ArrayType(1);
    end
    function v = T1_MAP
      v = mrd.ArrayType(2);
    end
    function v = T2_MAP
      v = mrd.ArrayType(3);
    end
    function v = T2STAR_MAP
      v = mrd.ArrayType(4);
    end
    function v = ADC_MAP
      v = mrd.ArrayType(5);
    end
    function v = B0_MAP
      v = mrd.ArrayType(6);
    end
    function v = B1_MAP
      v = mrd.ArrayType(7);
    end
    function v = SENSITIVITY_MAP
      v = mrd.ArrayType(8);
    end
    function v = GFACTOR_MAP
      v = mrd.ArrayType(9);
    end
    function v = USER_MAP
      v = mrd.ArrayType(10);
    end

    function z = zeros(varargin)
      elem = mrd.ArrayType(0);
      if nargin == 0
        z = elem;
        return;
      end
      sz = [varargin{:}];
      if isscalar(sz)
        sz = [sz, sz];
      end
      z = reshape(repelem(elem, prod(sz)), sz);
    end
  end
end
