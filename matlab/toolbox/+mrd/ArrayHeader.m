% This file was generated by the "yardl" tool. DO NOT EDIT.

classdef ArrayHeader < handle
  properties
    dimension_labels
    array_type
    meta
  end

  methods
    function self = ArrayHeader(kwargs)
      arguments
        kwargs.dimension_labels = mrd.ArrayDimension.empty();
        kwargs.array_type = yardl.None;
        kwargs.meta = yardl.Map;
      end
      self.dimension_labels = kwargs.dimension_labels;
      self.array_type = kwargs.array_type;
      self.meta = kwargs.meta;
    end

    function res = eq(self, other)
      res = ...
        isa(other, "mrd.ArrayHeader") && ...
        isequal({self.dimension_labels}, {other.dimension_labels}) && ...
        isequal({self.array_type}, {other.array_type}) && ...
        isequal({self.meta}, {other.meta});
    end

    function res = ne(self, other)
      res = ~self.eq(other);
    end

    function res = isequal(self, other)
      res = all(eq(self, other));
    end
  end

  methods (Static)
    function z = zeros(varargin)
      elem = mrd.ArrayHeader();
      if nargin == 0
        z = elem;
        return;
      end
      sz = [varargin{:}];
      if isscalar(sz)
        sz = [sz, sz];
      end
      z = reshape(repelem(elem, prod(sz)), sz);
    end
  end
end
